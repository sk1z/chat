name: firestore_repository
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.0.1

dependencies:
  local_cache:
    path: ../local_cache
  cloud_firestore: ^4.8.0
  equatable: ^2.0.5

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
